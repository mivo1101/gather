# Auth.js
AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# AUTH_URL=http://localhost:3000

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Postgres connection string from Supabase → Settings → Database → URI
DATABASE_URL=

# Unsplash stock photos (server-side only)
UNSPLASH_ACCESS_KEY=

# Transactional email (Resend)
RESEND_API_KEY=
# Verified sender, e.g. Gather <invitations@yourdomain.com>
# Defaults to Resend onboarding address for local testing.
EMAIL_FROM=
# Public app URL used in invite + email links (falls back to AUTH_URL)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
