import { ComingSoon } from "@/components/app/AppTopBar";

export const metadata = { title: "Brand Kit · Gather" };

export default function BrandKitPage() {
  return (
    <ComingSoon
      title="Brand Kit"
      description="Save your colours, fonts, and logos so every invitation stays on-brand."
    />
  );
}
