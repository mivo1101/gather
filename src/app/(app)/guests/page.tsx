import { ComingSoon } from "@/components/app/AppTopBar";

export const metadata = { title: "Guests · Gather" };

export default function GuestsPage() {
  return (
    <ComingSoon
      title="Guests"
      description="Import guest lists and send personalised invitation links."
    />
  );
}
