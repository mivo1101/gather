import { ComingSoon } from "@/components/app/AppTopBar";

export const metadata = { title: "Invitations · Gather" };

export default function InvitationsPage() {
  return (
    <ComingSoon
      title="Invitations"
      description="Browse and manage every invitation in one place. This section is next."
    />
  );
}
